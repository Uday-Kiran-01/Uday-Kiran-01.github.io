# Copy to .env and set the DatoCMS tokens for local dev
REACT_APP_DATOCMS_FRONTEND_TOKEN=
REACT_APP_DATOCMS_JAVA_TOKEN=
REACT_APP_DATOCMS_NODE_TOKEN=

# Notes:
# - Do NOT commit your real tokens. Use .env locally and add .env to .gitignore.
# - These tokens should be read-only tokens from DatoCMS for the corresponding hostnames.
